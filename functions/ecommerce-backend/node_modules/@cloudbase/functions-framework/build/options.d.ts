import { SignatureType } from './types';
export declare class ConfigurableOption<T> {
    readonly inputOption: string;
    readonly alias: string;
    private envVar;
    private defaultValue;
    private transform?;
    constructor(inputOption: string, alias: string, envVar: string, defaultValue: T, transform?: ((value: T | string) => T | Error) | undefined);
    parse(dict: Record<string, any>, envVars: NodeJS.ProcessEnv): T;
}
export interface FunctionDefinition {
    functionName: string;
    directory: string;
    sourceLocation: string;
    target: string;
}
export interface FrameworkOptions {
    /**
     * The port to listen on. Default: 3000.
     */
    port: number;
    /**
     * The target function name to load. Default: 'main'.
     */
    target: string;
    /**
     * Whether to load all functions in the 'functionsRoot' directory. Default: false.
     */
    loadAllFunctions: boolean;
    /**
     * The root directory of the functions. only effective when `loadAllFunctions` is true.
     */
    functionsRoot: string;
    /**
     * The relative path to the source file. Default: 'index.js'.
     */
    sourceLocation: string;
    /**
     * The signature type of the function. Default: 'event'.
     * Supported values: 'event'.
     */
    signatureType: SignatureType;
    /**
     * The maximum number of instantaneously incoming requests. 0 means no limit. Default: 0.
     * Requests exceeding this limit will receive a 429 response indicating the server is busy.
     */
    concurrencyLimit: number;
    /**
     * Node.js server timeout in milliseconds. Default: 0.
     */
    timeoutMS: number;
    /**
     * The keep-alive timeout in milliseconds. Default: 25000.
     */
    keepAliveTimeoutMS: number;
    /**
     * Whether server should wait for ongoing requests to complete before shutting down.
     * Default true when 'NODE_ENV=production' otherwise false.
     */
    gracefulShutdown: boolean;
    /**
     * If true, only validate the function and exit. Default: false.
     */
    dryRun: boolean;
    /**
     * If true, process.exit calls will have no effect. Default: false.
     */
    noExit: boolean;
    /**
     * If true, intercept console、process.stdout、process.stderr output. Default: false.
     */
    interceptOutput: boolean;
    /**
     * If true, stack traces of error will be returned in the response.
     * Default: if NODE_ENV is 'production', false; otherwise, true.
     */
    captureStack: boolean;
    /**
     * If true, request and response headers and body will be logged
     * into accesslog with body size limited to 1KB. Default: false.
     */
    logHeaderBody: boolean;
    /**
     * If true, log event and context function received in the access log. Default: false.
     */
    logEventContext?: boolean;
    /**
     * Whether to include simplified `__CONTENT__` field in logs. Default: true.
     */
    logContentEnabled?: boolean;
    /**
     * If true, print help text and exit. Default: false.
     */
    printHelp?: boolean;
    /**
     * The directory to store log files. Default: 'logs'.
     */
    logDirname?: string;
    /**
     * The maximum size of a log file in bytes. Default: 128MB.
     */
    maxLogFileSize?: number;
    /**
     * The maximum number of log files to keep. Default: 4.
     */
    maxLogFiles?: number;
    /**
     * The key of headers to parse `context.extendedContext`. `''` means that the feature is turned off, Default: ''.
     */
    extendedContextKey: string;
    extendedContext: object | null;
    /**
     * The multi-functions definition config file. Default: 'cloudbase-functions.json'.
     */
    functionsConfigFile: string;
    /**
     * If true, enable default CORS middleware for configured origins.
     */
    enableCors: boolean;
    /**
     * Allowed origins for CORS middleware. Only effective when `enableCors` is true.
     * Each Origin should be pure host without protocol or port. Default: ['localhost', '127.0.0.1'].
     * Wildcard origins are supported. e.g. ['*.example.com']
     */
    allowedOrigins?: string[];
    /**
     * The basic auth users. Default: [].
     */
    allowedAccounts?: {
        username: string;
        password: string;
    }[];
    /**
     * The path to the .env file. Default: '' means off. Support multiple paths separated by ','.
     * e.g. 'path/to/.env,path/to/.env.local'
     */
    dotEnvFilePath: string;
    /**
     * The root directory of static file. Default off.
     * e.g. 'public', 'static', 'dist'
     */
    staticServeRoot?: string | undefined;
    /**
     * The index of static file. Default off.
     * e.g. 'index.html'
     */
    staticServeIndex?: string | undefined;
    /**
     * The MaxAge of static file. Default 0.
     * e.g. '3600000' (1 hour), '86400000' (1 day)
     */
    staticServeMaxAge?: number;
}
export declare const helpText: string;
export declare const parseOptions: (cliArgs?: string[], envVars?: NodeJS.ProcessEnv) => FrameworkOptions;
//# sourceMappingURL=options.d.ts.map