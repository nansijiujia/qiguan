import * as http from 'http';
import { FrameworkOptions } from './options';
import { TKoaApp } from './types';
/**
 * createWebSocketServer 创建 WebSocket 服务，完成 websocket 握手后，调用用户函数
 * @param httpServer
 * @param koaApp
 * @param userFunction
 * @returns
 */
export declare function createWebSocketServer(httpServer: http.Server, koaApp: TKoaApp, options: FrameworkOptions): void;
//# sourceMappingURL=wss.d.ts.map